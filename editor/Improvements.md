This is an editor for the story-grimoire (my nontheistic name for story bible)

The world has many places and a place has many scenes

Right now a `world` has `places`
I think I want `Story` to replace what `World` is now, it's a root to hold many worlds. In all likelyhood there may only ever be one world but if there are dlc's or expansions, adding more worlds would make sense. They contain a set of places
Story->World-(many)>Places-(many)->Scenes

In game terms, a scene is a map. This represents what the player actually moves around in. This can be loaded and unloaded, and/or stitched together to create a larger area. 